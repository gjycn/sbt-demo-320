spring:
  threads:
    virtual:
      enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_db
    password: 123456
    username: root
  data:
    jdbc:
      repositories:
        enabled: true
    redis:
      repositories:
        enabled: true
      host: localhost
      port: 6379
      database: 0
      lettuce:
        pool:
          enabled: true